apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "concierge.fullname" . }}-test-connection"
  labels:
    {{- include "concierge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "concierge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
